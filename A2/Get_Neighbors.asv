function [ neighbors ] = Get_Neighbors( current_state )
   neighbors = [];
   x = current_state(1);
   y = current_state(2);

   if x > 1
   neighbors.add([x-1, y]);
   n2 = [x+1, y];
   n3 = [x, y+1];
   n4 = [x, y-1];
end

